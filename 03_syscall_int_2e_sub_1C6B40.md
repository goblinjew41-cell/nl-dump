# sub_1C6B40 — syscall через int 0x2e

**Адрес:** `0x1C6B40`
**Размер:** `0x30` (48 байт)
**Классификация:** leaf
**Особенность:** содержит inline `int 0x2e` syscall

## Найдено через

Поиск по опкоду `CD 2E` (int 0x2e) — найдено 5 вхождений в бинаре:

```
0x1c6b5a   ← в этой функции
0x3aedb1
0x3aedce
0x42e352
0x42e35c
```

## Декомпиляция

```c
__int64 sub_1C6B40()
{
  __int64 result;

  result = sub_18AD60(&dword_1770178,
                      954430638i64,    // arg2: 0x38E62A6E
                      1407848057i64,   // arg3: 0x53E2DA79
                      785244027i64);   // arg4: 0x2EC6177B
  dword_1770178 = 322376503;            // 0x1338_AA37
  return result;
}
```

Подозрительно, что `sub_18AD60` сама по себе — FNV-1a hash map (`0xCBF29CE484222325` — это константа FNV-1a). Здесь она используется для **lookup/insert с 3-аргументным ключом** (a2^a3^a4 = hash), что выглядит как cache-init или singleton-фабрика.

## Что нужно проверить дальше

Чтобы понять, что делает `int 0x2e` в этом месте:

1. **Дизассемблировать sub_1C6B40 полностью** через `disasm` — увидеть регистры перед `int 0x2e`. На Windows x64 `int 0x2e` устаревший метод syscall (для 32-bit или совместимости).
2. **Посмотреть соседей** — `0x1c6b5a` (int 0x2e), `0x3aedb1`, `0x3aedce` (рядом) — это скорее всего stub-ы для одной NtAPI.
3. **callgraph вверх** — кто зовёт `sub_1C6B40`? Если функция высокого уровня — это какой-то API-wrapper (например, `NtQuerySystemInformation` напрямую).

## Гипотезы (без runtime-trace не проверить)

- `sub_1C6B40` = кеш-инициализация для какого-то глобального состояния
- `int 0x2e` тут может быть fallback для WOW64-слоя (32-bit ntdll на 64-bit OS)

## IDB

`C:\Users\Zalman\Desktop\nl_cs2\NLCS2\NL\cs2_212C3300000.bin.i64`
Рекомендуемое имя: `cache_init_or_singleton` (после ручного уточнения).